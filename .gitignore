*.iml
.gradle/
.idea/
local.properties
.DS_Store
build/
captures/
.externalNativeBuild/
.cxx/

# Keystores / secrets — never check in
*.jks
*.keystore
google-services.json

# Room schema exports — MUST be tracked so MigrationTestHelper can read them in CI
# (Previously gitignored by mistake; re-enabled here)
# app/schemas/   ← intentionally NOT ignored

# Sensitive test artifacts and backup files — never commit these
*.sakuwise
*.csv
*.xlsx
test-scratch/
restore-dumps/
*.log

# Test reports + Paparazzi golden diff outputs
**/build/reports/
**/build/test-results/
**/paparazzi/snapshots-diff/

# Claude Code local state — agent transcripts/settings, not source
.claude/

# Dev-process docs (handoff/plan/audit) — keep local, never push to public repo
HANDOFF*.md
PLAN_*.md
SECURITY_AUDIT_*.md
