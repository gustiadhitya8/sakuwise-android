A hardening release — focused on data safety & quality, no feature changes:

• Database migrations are now explicit & tested — your data won't be wiped on a future update
• Older backups still restore on new versions (backup format is versioned & tested)
• CSV/XLSX export ↔ import now match exactly — lossless round-trip
• English mode no longer leaks Indonesian text
• Faster cold start (baseline profile) + crash monitoring via Android Vitals, no tracking